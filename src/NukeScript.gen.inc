// AUTO-GENERATED by NukeUtils/nukegen.py — DO NOT EDIT.
// #included IN-TU after the module's component definitions (no headers here).
namespace nuke {
// Registers this MODULE's reflected components into the engine's shared registry.
// Call once from the module's NUKEModule::OnLoad (before any world deserializes).
bool NukeReflectInit_NukeScript() {
	static bool _done = false;
	if (_done) return true;
	_done = true;
	{
		TypeInfo& t = TypeOf<ScriptComponent>();
		t.base = "Component";
		t.category = "Scripts";
		t.fields.push_back(MakeField("script", &ScriptComponent::script, "script"));
		t.fields.push_back(MakeField("props", &ScriptComponent::props));
		t.fields.back().hidden = true;
		t.create = []() -> void* { return new ScriptComponent(); };
	}
	return true;
}
}  // namespace nuke
